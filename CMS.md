# CMS — پنل مدیریت نویدیکس

این فایل می‌گوید پنل از چه ساخته شده، چطور راهش بیندازی، و مهم‌تر از هر دو:
**چه چیزی هنوز وصل نیست و چرا.**

---

## ۱. راه‌اندازی — به همین ترتیب

در Supabase → SQL Editor، محتوای هر فایل را کپی کن و Run بزن. هر پنج فایل
idempotent‌اند: دوباره اجرا کردنشان نه خطا می‌دهد و نه داده‌ای را پاک می‌کند.

| # | فایل | چه می‌سازد |
|---|---|---|
| ۱ | `supabase/schema.sql` | پروفایل، پیشرفت درس، بازدید صفحه، نماهای آمار |
| ۲ | `supabase/users-and-refs.sql` | فهرست کاربران و منابع ورودی |
| ۳ | `supabase/fix-advisor.sql` | پاسخ به هشدارهای Advisor |
| ۴ | `supabase/cms-rbac.sql` | نقش‌ها، Permissionها، گزارش فعالیت، تنظیمات، رسانه |
| ۵ | `supabase/cms-content.sql` | مقاله، پرامپت، دوره/فصل/درس، گالری، ویدیو، جست‌وجو |

اگر فقط ۱ تا ۳ را قبلاً اجرا کرده‌ای، از ۴ شروع کن.

### آخرین قدم — خودت را Owner کن

تا این اجرا نشود هیچ‌کس Owner نیست، و این تنها راهش است — از مرورگر ممکن نیست:

```sql
update public.profiles set role = 'owner'
where id = (select id from auth.users where email = 'ایمیل‌ات');
```

**اول مطمئن شو حسابت وجود دارد.** این را بزن:

```sql
select u.id, u.email,
       (u.email_confirmed_at is not null) as confirmed,
       (p.id is not null)                 as has_profile,
       p.role
from auth.users u
left join public.profiles p on p.id = u.id
order by u.created_at;
```

- **صفر ردیف** → هنوز در خود سایت ثبت‌نام نکرده‌ای. برو به سایت و از دکمه‌ی
  «ورود / ثبت‌نام» حساب بساز، بعد برگرد.
- **ردیف هست ولی `has_profile = false`** → حساب قبل از ساخته‌شدن تریگر ساخته
  شده. این یک بار اجرا شود و درست می‌شود:
  ```sql
  insert into public.profiles (id)
  select u.id from auth.users u
  left join public.profiles p on p.id = u.id
  where p.id is null;
  ```

### زمان‌بندی انتشار (اختیاری)

بدون این، محتوای زمان‌بندی‌شده در موعدش خودکار منتشر **نمی‌شود**. اول در
Database → Extensions افزونه‌ی `pg_cron` را روشن کن، بعد:

```sql
select cron.schedule('nvx-publish-due', '*/5 * * * *',
                     $$select public.publish_due()$$);
```

عمداً داخل فایل SQL کامنت است: اگر افزونه روشن نباشد، اجرای آن خط کل فایل را
متوقف می‌کند.

---

## ۲. نقش‌ها و دسترسی‌ها

| نقش | دسترسی‌ها |
|---|---|
| **owner** | همه‌ی ۱۴ دسترسی + تنها کسی که گزارش فعالیت را می‌بیند |
| **admin** | همه به‌جز `admins.manage`، `roles.manage`، `audit.view` |
| **editor** | همه‌ی محتوا + انتشار + رسانه |
| **writer** | `content.create`، `content.edit`، `media.manage` — بدون حذف، بدون انتشار |
| **viewer** | فقط می‌بیند |
| **reader** | کاربر عادی سایت، اصلاً وارد پنل نمی‌شود |

نقشه‌ی نقش→دسترسی در جدول `role_permissions` است و از SQL Editor قابل تغییر.

### امنیت — چه چیزی واقعاً جلوی چه کسی را می‌گیرد

مخفی‌کردن دکمه در پنل **ادب است، نه امنیت**. آنچه واقعاً اجرا می‌شود:

- `profiles.role` و `profiles.is_active` اصلاً در grant کلاینت نیستند؛ تنها
  راه تغییرشان `set_user_role()` و `set_user_active()` است که خودشان سه قفل
  دارند (خودت را عوض نمی‌کنی، `admins.manage` لازم است، Owner فقط با Owner).
- انتشار با یک تریگر جدا از نوشتن کنترل می‌شود، پس Writer می‌تواند بنویسد و
  هرگز نتواند منتشر کند.
- محتوای زمان‌بندی‌شده تا موعدش از بیرون دیده نمی‌شود، چون سیاست خواندن شرط
  `published_at <= now()` را دارد. بدترین حالت نرسیدن cron «دیر منتشر شد» است،
  نه «زودتر لو رفت».
- گزارش فعالیت فقط از تابع نوشته می‌شود و actor همیشه از خود توکن گرفته
  می‌شود — یک سطر گزارش را نمی‌شود به اسم کس دیگری ثبت کرد.

این‌ها روی یک PostgreSQL واقعی تست شده‌اند، نه فقط ادعا.

### کلیدها

`nvx-config.js` فقط کلید publishable دارد که ساخته شده تا در مرورگر باشد.
**هیچ‌وقت** کلید `sb_secret_…` یا رمز پایگاه داده وارد این مخزن نشود — آن‌ها
همه‌ی سیاست‌های بالا را دور می‌زنند.

---

## ۳. چه چیزی وصل است و چه چیزی نه

این مهم‌ترین بخش این فایل است.

### وصل است ✅

| | |
|---|---|
| `articles.html` | فهرست مقاله‌های منتشرشده، مستقیم از پایگاه داده |
| `article.html?slug=…` | یک مقاله، با Markdown، عنوان و متای پویا |

هر دو از `nvx-content.js` می‌خوانند و **از روز اول** پایگاه‌داده‌محورند. هیچ
سازنده‌ای صاحبشان نیست.

### وصل نیست ❌

`prompts.html`، `gallery.html`، `documentaries.html`، `collections.html`،
`channels.html`، `training.html` و `index.html` هنوز محتوایشان را از مخزن
می‌گیرند، نه از پایگاه داده. یعنی پرامپتی که در پنل می‌سازی، در
`prompts.html` ظاهر نمی‌شود.

**چرا دستی وصلشان نکردم:** این هفت فایل را `tools/build-sections.py` و
`tools/build-library.py` بازتولید می‌کنند. هر تغییر دستی در خودشان با اولین
build پاک می‌شود — همان تله‌ای که `tools/README.md` درباره‌اش هشدار داده.

### چطور وصلشان کنی

تغییر باید در **قالب سازنده** باشد، نه در خروجی. دو خط، در جایی از قالب که
می‌خواهی محتوا ظاهر شود:

```html
<script src="nvx-content.js" defer></script>
<div data-nvx="prompts" data-nvx-limit="12"></div>
```

مقدارهای ممکن `data-nvx`: `articles` · `prompts` · `gallery` · `videos` ·
`lessons`

صفت‌های اختیاری:

| صفت | کار |
|---|---|
| `data-nvx-limit="12"` | حداکثر تعداد |
| `data-nvx-featured="true"` | فقط موارد ویژه |
| `data-nvx-keep` | وقتی چیزی منتشر نشده، جای خالی بماند |
| `data-nvx-hide=".section"` | وقتی خالی است، کل بخش والد پنهان شود |

اگر چیزی منتشر نشده باشد، مقصد **خودش را حذف می‌کند** — پس اضافه‌کردن این دو
خط به یک صفحه‌ی زنده تا روزی که واقعاً محتوایی منتشر کنی هیچ اثری ندارد.

⚠️ **قبل از اجرای هر سازنده‌ای `git diff` را بخوان.** طبق `tools/README.md`
دست‌کم چهار سازنده از خروجی‌شان عقب‌ترند و اجرایشان یک قدم به عقب است.

---

## ۴. SEO — هزینه‌ای که آگاهانه پرداخت شده

`articles.html` و `article.html` در مرورگر رندر می‌شوند. یعنی خزنده‌ای که
JavaScript اجرا نمی‌کند، پوسته را می‌بیند نه مقاله را. گوگل امروز JS اجرا
می‌کند، ولی این هنوز از HTML آماده ضعیف‌تر است.

این عمدی است: انتخاب بین «امروز از پنل منتشر کن» و «منتظر مرحله‌ی ساخت
استاتیک بمان» بود. راه‌حل کامل — یعنی Webhook از پایگاه داده که یک GitHub
Action را بیدار کند و صفحه‌ها را استاتیک بسازد — فاز بعدی است. آن موقع همین
دو فایل خروجی آن مرحله می‌شوند.

---

## ۵. فایل‌ها

### پنل (`admin.html` و ماژول‌هایش)

| فایل | کار |
|---|---|
| `nvx-admin.css` | استایل‌های CMS — مکمل بلوک CSS داخل `admin.html` |
| `nvx-admin-core.js` | گیت دسترسی، مسیریاب، db/storage، toast/modal |
| `nvx-admin-resource.js` | موتور CRUD عمومی — فهرست، فرم، وضعیت، ترتیب |
| `nvx-admin-content.js` | توصیف هفت نوع محتوا (منطقی ندارد، فقط توصیف) |
| `nvx-admin-dashboard.js` | اعداد بالای پنل |
| `nvx-admin-media.js` | کتابخانه‌ی رسانه + انتخابگر تصویر |
| `nvx-admin-search.js` | جست‌وجوی مرکزی |
| `nvx-admin-users.js` | نقش و فعال/غیرفعال |
| `nvx-admin-settings.js` | تنظیمات سایت |
| `nvx-admin-audit.js` | گزارش فعالیت (فقط Owner) |
| `nvx-admin-analytics.js` | آمار قدیمی، حالا به‌عنوان یک تب |

کد نمودارهای آمار همچنان inline داخل `admin.html` است و دست نخورده — فقط
`boot()` صادر شد تا پوسته وقتی آن تب باز شد صدایش بزند.

### سایت عمومی

| فایل | کار |
|---|---|
| `nvx-content.js` | خواندن محتوای منتشرشده با کلید anon، Markdown امن، تنظیمات |
| `articles.html` | فهرست مقاله‌ها |
| `article.html` | یک مقاله |

---

## ۶. اگر پنل چیزی نشان نداد

پنل طوری نوشته شده که هیچ‌وقت سفید نماند و همیشه بگوید چه کم است:

| چه می‌بینی | معنی |
|---|---|
| بنر «بخش‌های CMS هنوز فعال نیستند» | `cms-rbac.sql` اجرا نشده |
| «این بخش هنوز در پایگاه داده ساخته نشده» | `cms-content.sql` اجرا نشده |
| «این حساب دسترسی مدیر ندارد» | نقشت هنوز `reader` است |
| «این حساب غیرفعال شده است» | `is_active = false` |
| فقط تب «آمار» | حالت legacy — یعنی مورد اول |
