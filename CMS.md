# CMS — پنل مدیریت نویدیکس

این فایل می‌گوید پنل از چه ساخته شده، چطور راهش بیندازی، و مهم‌تر از هر دو:
**چه چیزی هنوز وصل نیست و چرا.**

---

## ۱. راه‌اندازی

### راه ساده — یک فایل، یک بار

```
supabase/install.sql
```

کل محتوایش را کپی کن و در Supabase → SQL Editor یک بار Run بزن. همین. هر شش
فایل زیر را به ترتیب درست در خودش دارد، و آخرش هم فهرست حساب‌ها را نشانت
می‌دهد تا بلافاصله بفهمی حسابت ثبت شده یا نه.

روی یک PostgreSQL واقعی از صفر اجرا و دو بار تکرار شده: ۲۲ جدول، ۵۹ تابع،
۶۴ سیاست RLS — بدون خطا، بدون از دست رفتن داده.

> این فایل **ساخته می‌شود**. اگر یکی از شش فایل زیر را عوض کردی،
> `python3 tools/build-install-sql.py` را بزن تا دوباره ساخته شود.

### راه تکه‌تکه — اگر بخواهی جدا اجرا کنی

هر شش فایل idempotent‌اند: دوباره اجرا کردنشان نه خطا می‌دهد و نه داده‌ای را
پاک می‌کند.

| # | فایل | چه می‌سازد |
|---|---|---|
| ۱ | `supabase/schema.sql` | پروفایل، پیشرفت درس، بازدید صفحه، نماهای آمار |
| ۲ | `supabase/users-and-refs.sql` | فهرست کاربران و منابع ورودی |
| ۳ | `supabase/fix-advisor.sql` | پاسخ به هشدارهای Advisor |
| ۴ | `supabase/cms-rbac.sql` | نقش‌ها، Permissionها، گزارش فعالیت، تنظیمات، رسانه |
| ۵ | `supabase/cms-content.sql` | مقاله، پرامپت، دوره/فصل/درس، گالری، ویدیو، جست‌وجو |
| ۶ | `supabase/cms-ai.sql` | دستیار هوش مصنوعی، انتشار خودکار، ورک‌فلوها |

اگر فقط ۱ تا ۳ را قبلاً اجرا کرده‌ای، از ۴ شروع کن — یا همان `install.sql` را
بزن که هر شش تا را پوشش می‌دهد.

### دستیار هوش مصنوعی — یک قدم اضافه

جدول‌ها با فایل ۶ ساخته می‌شوند، ولی خودِ صداکردن مدل کار یک Edge Function
است. **کلید مدل هیچ‌وقت نباید در مرورگر باشد**، پس جایش آنجاست:

```bash
supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
supabase functions deploy ai-assistant
```

تا وقتی این اجرا نشود، بخش «دستیار محتوا» باز می‌شود ولی هر درخواست با پیامی
که همین دستور را نام می‌برد رد می‌شود — نه یک خطای شبکه‌ی مبهم.

### آخرین قدم — خودت را Owner کن

تا این اجرا نشود هیچ‌کس Owner نیست، و این تنها راهش است — از مرورگر ممکن نیست:

```sql
update public.profiles set role = 'owner'
where id = (select id from auth.users where email = 'ایمیل‌ات');
```

**اول مطمئن شو حسابت وجود دارد.** این را بزن:

```sql
select u.id, u.email,
       (u.email_confirmed_at is not null) as confirmed,
       (p.id is not null)                 as has_profile,
       p.role
from auth.users u
left join public.profiles p on p.id = u.id
order by u.created_at;
```

- **صفر ردیف** → هنوز در خود سایت ثبت‌نام نکرده‌ای. برو به سایت و از دکمه‌ی
  «ورود / ثبت‌نام» حساب بساز، بعد برگرد.
- **ردیف هست ولی `has_profile = false`** → حساب قبل از ساخته‌شدن تریگر ساخته
  شده. این یک بار اجرا شود و درست می‌شود:
  ```sql
  insert into public.profiles (id)
  select u.id from auth.users u
  left join public.profiles p on p.id = u.id
  where p.id is null;
  ```

### زمان‌بندی انتشار (اختیاری)

بدون این، محتوای زمان‌بندی‌شده در موعدش خودکار منتشر **نمی‌شود**. اول در
Database → Extensions افزونه‌ی `pg_cron` را روشن کن، بعد:

```sql
select cron.schedule('nvx-publish-due', '*/5 * * * *',
                     $$select public.publish_due()$$);
```

عمداً داخل فایل SQL کامنت است: اگر افزونه روشن نباشد، اجرای آن خط کل فایل را
متوقف می‌کند.

---

## ۲. نقش‌ها و دسترسی‌ها

| نقش | دسترسی‌ها |
|---|---|
| **owner** | همه‌ی ۱۴ دسترسی + تنها کسی که گزارش فعالیت را می‌بیند |
| **admin** | همه به‌جز `admins.manage`، `roles.manage`، `audit.view` |
| **editor** | همه‌ی محتوا + انتشار + رسانه |
| **writer** | `content.create`، `content.edit`، `media.manage` — بدون حذف، بدون انتشار |
| **viewer** | فقط می‌بیند |
| **reader** | کاربر عادی سایت، اصلاً وارد پنل نمی‌شود |

نقشه‌ی نقش→دسترسی در جدول `role_permissions` است و از SQL Editor قابل تغییر.

### امنیت — چه چیزی واقعاً جلوی چه کسی را می‌گیرد

مخفی‌کردن دکمه در پنل **ادب است، نه امنیت**. آنچه واقعاً اجرا می‌شود:

- `profiles.role` و `profiles.is_active` اصلاً در grant کلاینت نیستند؛ تنها
  راه تغییرشان `set_user_role()` و `set_user_active()` است که خودشان سه قفل
  دارند (خودت را عوض نمی‌کنی، `admins.manage` لازم است، Owner فقط با Owner).
- انتشار با یک تریگر جدا از نوشتن کنترل می‌شود، پس Writer می‌تواند بنویسد و
  هرگز نتواند منتشر کند.
- محتوای زمان‌بندی‌شده تا موعدش از بیرون دیده نمی‌شود، چون سیاست خواندن شرط
  `published_at <= now()` را دارد. بدترین حالت نرسیدن cron «دیر منتشر شد» است،
  نه «زودتر لو رفت».
- گزارش فعالیت فقط از تابع نوشته می‌شود و actor همیشه از خود توکن گرفته
  می‌شود — یک سطر گزارش را نمی‌شود به اسم کس دیگری ثبت کرد.

این‌ها روی یک PostgreSQL واقعی تست شده‌اند، نه فقط ادعا.

### کلیدها

`nvx-config.js` فقط کلید publishable دارد که ساخته شده تا در مرورگر باشد.
**هیچ‌وقت** کلید `sb_secret_…` یا رمز پایگاه داده وارد این مخزن نشود — آن‌ها
همه‌ی سیاست‌های بالا را دور می‌زنند.

---

## ۳. چه چیزی وصل است و چه چیزی نه

این مهم‌ترین بخش این فایل است.

### وصل است ✅

| صفحه | چه چیزی از پنل می‌آید |
|---|---|
| `articles.html` | فهرست مقاله‌های منتشرشده |
| `article.html?slug=…` | یک مقاله، با Markdown، عنوان و متای پویا |
| `index.html` | نوار «نوشته‌های استودیو» — چهار مقاله‌ی تازه با تصویر شاخص |
| `documentaries.html` | ویدیوهای منتشرشده، در همان شبکه و با همان کارت صفحه |
| `prompts.html` | پرامپت‌های پنل، در بخش «پرامپت‌های تازه» — در جست‌وجو و فیلتر همان صفحه هم شرکت می‌کنند |
| `gallery.html` | تصویرهای پنل، در بخش «افزوده‌های استودیو» بالای خط زمان |
| `channels.html` | فهرست شبکه‌های اجتماعی، از جدول `social_links` |
| `about.html` | نام، عنوان و متن معرفی، از بخش «معرفی» پنل |
| `system.html` | خدماتی که در پنل ساخته می‌شوند، در بخش «بسته‌های دیگر استودیو» |
| **هر صفحه‌ی سایت** | لینک شبکه‌های اجتماعی. اگر آیدی‌ات را در پنل عوض کنی، همه‌ی ۲۲۰ صفحه با هم عوض می‌شوند |

یک قاعده روی همه‌ی این‌ها: **تا وقتی جدول خالی است، صفحه همان چیزی است که
بود.** هیچ‌کدام از این بخش‌ها متن دست‌نویس صفحه را پاک نمی‌کند مگر آنکه
واقعاً ردیفی برای نشان‌دادن آمده باشد — پس اگر پایگاه داده هم از دسترس خارج
شود، سایت چیزی کم ندارد.

#### لینک شبکه‌های اجتماعی، دقیق‌تر

عوض‌کردن یک آیدی نباید به معنی ساختن دوباره‌ی ۲۲۰ صفحه باشد. برای همین
`nvx-content.js` روی هر صفحه، هر لینکی را که **از قبل** به همان شبکه می‌رفته
با نشانی تازه هم‌خط می‌کند — و آیدیِ کنارش را هم. شرطش این است که میزبان
(host) لینک قدیمی با میزبان لینک تازه یکی باشد، پس این کار نمی‌تواند لینکی
به جای دیگر را به حساب اجتماعی تبدیل کند و نمی‌تواند لینکی بسازد که صفحه
نداشته.

شبکه‌ی تازه هم مشکلی ندارد: `platform` متن آزاد است، آیکون از روی همان
انتخاب می‌شود، و هرچه ناشناس باشد یک آیکون عمومی می‌گیرد.

---

## ۳.۵ دو راه انتشار مقاله — هر دو زنده

مقاله از **دو مسیر** به سایت می‌رسد و هیچ‌کدام جای دیگری را نمی‌گیرد:

| مسیر | برای چه کسی | کجا می‌نشیند |
|---|---|---|
| **مخزن** | هر عاملی که فقط به گیت دسترسی دارد | `content/articles/*.md` |
| **پنل** | آدم | جدول `articles` |

`nvx-content.js` هر دو را می‌خواند و در یک فهرست، تازه‌ترین اول، نشان می‌دهد.

**چرا هر دو لازم است:** پنل برای آدم عالی است و برای یک عامل خودکار بی‌فایده —
کسی که فقط به مخزن دسترسی دارد نمی‌تواند در پایگاه داده بنویسد. وقتی مقاله‌ها
فقط در پایگاه داده بودند، «یک مقاله بذار» از یک کار یک‌مرحله‌ای به کاری تبدیل
شد که آخرش دست صاحب سایت بود. مسیر فایل همان یک‌مرحله‌ای بودن را برمی‌گرداند.

### انتشار از مخزن

یک فایل در `content/articles/` بساز:

```markdown
---
title: عنوان مقاله
slug: my-article
date: 2026-08-19
excerpt: یکی دو جمله خلاصه.
tags: هوش مصنوعی, ویدیو
seo_title: عنوان کوتاه‌تر برای گوگل
seo_description: توضیح متا.
---

متن مقاله به Markdown.
```

بعد:

```bash
python3 tools/build-content.py
```

که `content/articles.json` را می‌سازد. آن را هم commit کن. همین.

اگر سربرگ ایراد داشته باشد، اسکریپت می‌گوید کدام فایل و چه ایرادی — و
`articles.json` را دست‌نخورده می‌گذارد.

### وقتی یک slug در هر دو باشد

نسخه‌ی **پایگاه داده** برنده است، و در فهرست هم تکراری نمی‌شود. منطقش این است
که آن یکی را یک آدم آگاهانه ویرایش کرده و فایل فقط جایی است که شروع شده.

### وصل نیست ❌

`prompts.html`، `gallery.html`، `documentaries.html`، `collections.html`،
`channels.html`، `training.html` و `index.html` هنوز محتوایشان را از مخزن
می‌گیرند، نه از پایگاه داده. یعنی پرامپتی که در پنل می‌سازی، در
`prompts.html` ظاهر نمی‌شود.

**چرا دستی وصلشان نکردم:** این هفت فایل را `tools/build-sections.py` و
`tools/build-library.py` بازتولید می‌کنند. هر تغییر دستی در خودشان با اولین
build پاک می‌شود — همان تله‌ای که `tools/README.md` درباره‌اش هشدار داده.

### چطور وصلشان کنی

تغییر باید در **قالب سازنده** باشد، نه در خروجی. دو خط، در جایی از قالب که
می‌خواهی محتوا ظاهر شود:

```html
<script src="nvx-content.js" defer></script>
<div data-nvx="prompts" data-nvx-limit="12"></div>
```

مقدارهای ممکن `data-nvx`: `articles` · `prompts` · `gallery` · `videos` ·
`lessons`

صفت‌های اختیاری:

| صفت | کار |
|---|---|
| `data-nvx-limit="12"` | حداکثر تعداد |
| `data-nvx-featured="true"` | فقط موارد ویژه |
| `data-nvx-keep` | وقتی چیزی منتشر نشده، جای خالی بماند |
| `data-nvx-hide=".section"` | وقتی خالی است، کل بخش والد پنهان شود |

اگر چیزی منتشر نشده باشد، مقصد **خودش را حذف می‌کند** — پس اضافه‌کردن این دو
خط به یک صفحه‌ی زنده تا روزی که واقعاً محتوایی منتشر کنی هیچ اثری ندارد.

⚠️ **قبل از اجرای هر سازنده‌ای `git diff` را بخوان.** طبق `tools/README.md`
دست‌کم چهار سازنده از خروجی‌شان عقب‌ترند و اجرایشان یک قدم به عقب است.

---

## ۴. SEO — هزینه‌ای که آگاهانه پرداخت شده

`articles.html` و `article.html` در مرورگر رندر می‌شوند. یعنی خزنده‌ای که
JavaScript اجرا نمی‌کند، پوسته را می‌بیند نه مقاله را. گوگل امروز JS اجرا
می‌کند، ولی این هنوز از HTML آماده ضعیف‌تر است.

این عمدی است: انتخاب بین «امروز از پنل منتشر کن» و «منتظر مرحله‌ی ساخت
استاتیک بمان» بود. راه‌حل کامل — یعنی Webhook از پایگاه داده که یک GitHub
Action را بیدار کند و صفحه‌ها را استاتیک بسازد — فاز بعدی است. آن موقع همین
دو فایل خروجی آن مرحله می‌شوند.

---

## ۵. فایل‌ها

### پنل (`admin.html` و ماژول‌هایش)

| فایل | کار |
|---|---|
| `nvx-admin.css` | استایل‌های CMS — مکمل بلوک CSS داخل `admin.html` |
| `nvx-admin-core.js` | گیت دسترسی، مسیریاب، db/storage، toast/modal |
| `nvx-admin-resource.js` | موتور CRUD عمومی — فهرست، فرم، وضعیت، ترتیب |
| `nvx-admin-content.js` | توصیف هفت نوع محتوا (منطقی ندارد، فقط توصیف) |
| `nvx-admin-dashboard.js` | اعداد بالای پنل |
| `nvx-admin-media.js` | کتابخانه‌ی رسانه + انتخابگر تصویر |
| `nvx-admin-search.js` | جست‌وجوی مرکزی |
| `nvx-admin-users.js` | نقش و فعال/غیرفعال |
| `nvx-admin-settings.js` | تنظیمات سایت |
| `nvx-admin-audit.js` | گزارش فعالیت (فقط Owner) |
| `nvx-admin-analytics.js` | آمار قدیمی، حالا به‌عنوان یک مقصد در ریل |
| `nvx-admin-icons.js` | ۲۵ آیکون، به‌صورت path — بدون وابستگی بیرونی |
| `nvx-admin-ai.js` | دستیار محتوا + کلیدهای انتشار خودکار |
| `nvx-admin-workflows.js` | ورک‌فلوها و تاریخچه‌ی اجراها |
| `supabase/functions/ai-assistant/` | تنها جایی که کلید مدل زندگی می‌کند |

کد نمودارهای آمار همچنان inline داخل `admin.html` است و دست نخورده — فقط
`boot()` صادر شد تا پوسته وقتی آن تب باز شد صدایش بزند.

### سایت عمومی

| فایل | کار |
|---|---|
| `nvx-content.js` | خواندن محتوای منتشرشده با کلید anon، Markdown امن، تنظیمات |
| `articles.html` | فهرست مقاله‌ها |
| `article.html` | یک مقاله |

---

## ۶. هوش مصنوعی — چه چیزی تضمین است و چه چیزی نه

**مدل هیچ‌وقت مستقیم منتشر نمی‌کند.** این یک قول نیست، یک واقعیت ساختاری است:
Edge Function اصلاً مسیری به جدول‌های محتوا ندارد. خروجی مدل در `ai_jobs`
می‌نشیند و تنها راهی که به `articles` می‌رسد تابع `ai_apply_job()` است، که در
پایگاه داده و با دسترسی خودِ کاربر اجرا می‌شود و همیشه پیش‌نویس می‌سازد.

انتشار خودکار را می‌شود برای هر نوع محتوا جدا روشن کرد، ولی آن هم دور نمی‌زند:
محتوا به **زمان‌بندی‌شده** می‌رود با تأخیری که خودت تعیین می‌کنی (پیش‌فرض ۳۰
دقیقه)، تا اگر خروجی بد بود فرصت جلوگیری داشته باشی. و روشن‌کردن همان کلید
`content.publish` می‌خواهد.

روی PostgreSQL واقعی تست شده: یک Writer **حتی با انتشار خودکار روشن** باز هم
فقط پیش‌نویس می‌سازد، چون `ai_apply_job` دسترسی انتشار را از خودِ کاربر
می‌خواهد نه از تنظیمات.

### ورک‌فلوها

`ai_start_run()` فقط کار را در صف می‌گذارد — Postgres نه به اینترنت وصل است و
نه جای نگه‌داشتن کلید مدل. اجرایی که در «در حال اجرا» مانده یعنی هیچ‌کس آن را
برنداشته (تابع مستقر نشده). این حالت در پنل دیده می‌شود و ساکت نمی‌ماند.

زمان‌بندی cron هم فقط **ذخیره** می‌شود؛ وصل‌کردنش به pg_cron یک قدم دستی و
عمدی است. یک ورک‌فلوی خودکارِ اشتباه، هر ساعت اشتباه می‌کند.

---

## ۷. اگر پنل چیزی نشان نداد

پنل طوری نوشته شده که هیچ‌وقت سفید نماند و همیشه بگوید چه کم است:

| چه می‌بینی | معنی |
|---|---|
| بنر «بخش‌های CMS هنوز فعال نیستند» | `cms-rbac.sql` اجرا نشده |
| «این بخش هنوز در پایگاه داده ساخته نشده» | `cms-content.sql` اجرا نشده |
| «این حساب دسترسی مدیر ندارد» | نقشت هنوز `reader` است |
| «این حساب غیرفعال شده است» | `is_active = false` |
| فقط «آمار» در ریل | حالت legacy — یعنی مورد اول |
| «تابع ai-assistant مستقر نشده» | `supabase functions deploy ai-assistant` |

---

## ۸. رابط کاربری

پنل یک پوسته‌ی برنامه است، نه یک صفحه: ریل گروه‌بندی‌شده در سمت راست، هدر
چسبان که همیشه می‌گوید کجایی، و یک Command Palette روی همه‌شان.

- **⌘K** (یا Ctrl+K) هر جای پنل باز می‌شود و هم بخش‌ها را می‌گردد هم محتوا را.
  با ↑↓ حرکت کن و Enter برو.
- زیر ۸۶۰ پیکسل ریل به یک کشو تبدیل می‌شود، نه یک ستون فشرده.
- ویرایشگر دوستونی است: متن یک طرف، تصمیم‌ها (وضعیت، دسته، تگ، کاور) طرف
  دیگر. نوار ذخیره به پایین صفحه چسبیده، پس روی یک مقاله‌ی بلند هم یک کلیک
  فاصله دارد.
- رنگ معنا دارد: قرمز برند فقط روی عمل اصلی، آبی برای داده، و سبز/کهربایی/قرمز
  برای وضعیت. هر چیز ساختاری خاکستری است.
